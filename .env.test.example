# E2E test environment — copy to .env.test
# Uses separate database to avoid polluting dev data
DATABASE_URL="postgresql://mindshed:mindshed@localhost:5432/mindshed_test"

# Object Storage (same MinIO instance as dev)
R2_ENDPOINT="http://localhost:9000"
R2_ACCESS_KEY_ID="mindshed"
R2_SECRET_ACCESS_KEY="mindshed123"
R2_BUCKET_NAME="mindshed-images"

# Auth (any non-empty string enables auth for E2E)
APP_SECRET=e2e-test-secret
